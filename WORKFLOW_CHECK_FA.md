# بررسی Workflow گیت‌هاب

فایل Workflow باید دقیقاً در این مسیر باشد:

```text
.github/workflows/android.yml
```

در GitHub از تب Code روی این مسیر کلیک کنید و مطمئن شوید فایل دیده می‌شود. سپس در تب Actions، Workflow با نام `Build Android APK` ظاهر خواهد شد.

اگر هنگام آپلود با مرورگر پوشه `.github` نمایش داده نشد، فایل `android.yml` جداگانه را دانلود کنید و در GitHub با گزینه **Add file → Create new file**، نام فایل را دقیقاً این‌طور وارد کنید:

```text
.github/workflows/android.yml
```

سپس محتوای فایل را Paste و Commit کنید.
